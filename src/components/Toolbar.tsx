import React, { useState } from 'react';\nimport './Toolbar.css';  // Assuming you'll create a CSS file for styling  \n\nconst Toolbar = () => {\n    const [color, setColor] = useState('#000000'); // Default color\n    const [lineThickness, setLineThickness] = useState(1); // Default line thickness\n    const [opacity, setOpacity] = useState(1); // Default opacity\n    const tools = [ 'Free Draw', 'Straight Line', 'Angle Tool', 'Distance Tool', 'Circle', 'Rectangle', 'Ellipse', 'Dot-to-Dot', 'Text', 'Eraser', 'Undo', 'Redo' ]; // Define tools\n\n    return (\n        <div className='toolbar' style={{ backdropFilter: 'blur(10px)', backgroundColor: 'rgba(255, 255, 255, 0.8)' }}>\n            <div className='tools'>\n                {tools.map((tool) => (\n                    <button key={tool} className='tool-button'>{tool}</button>\n                ))}  \n            </div>\n            <input type='color' value={color} onChange={(e) => setColor(e.target.value)} title='Color Picker' />\n            <input type='range' min='1' max='10' value={lineThickness} onChange={(e) => setLineThickness(e.target.value)} title='Line Thickness' />\n            <input type='range' min='0' max='1' step='0.1' value={opacity} onChange={(e) => setOpacity(e.target.value)} title='Opacity' />\n        </div>\n    );\n};\n\nexport default Toolbar;
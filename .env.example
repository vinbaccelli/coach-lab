NEXT_PUBLIC_SUPABASE_URL=https://kgifsczgikzgeayueede.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Google OAuth client for YouTube import flow
NEXT_PUBLIC_GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID

# Optional: override YouTube resolve endpoint (defaults to same-origin /api/youtube/resolve on Node).
# Cloudflare Workers often cannot complete decipher + InnerTube from DC IPs; leave unset on Vercel.
NEXT_PUBLIC_YT_RESOLVER_URL=https://your-worker.example.workers.dev


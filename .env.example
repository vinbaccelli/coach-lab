NEXT_PUBLIC_SUPABASE_URL=https://kgifsczgikzgeayueede.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Google OAuth client for YouTube import flow
NEXT_PUBLIC_GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID

# YouTube resolution uses same-origin /api/youtube/resolve (see app/api/youtube/resolve/route.ts).
# Do not point the UI at a Cloudflare Worker — NEXT_PUBLIC_* is baked into the client at build time.

